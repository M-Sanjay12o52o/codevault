generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

model CodeSnippet {
  id      Int       @id @default(autoincrement())
  username String
  codeLanguage  String
  stdin    String?
  sourceCode String
}
